name: Docs

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt
      - run: mkdocs build --strict
      - name: Check site output
        run: |
          set -eux
          for f in index.html ru/index.html get-started/index.html ru/get-started/index.html \
                   providers/docker/resources/index.html ru/providers/docker/resources/index.html \
                   providers/kubernetes/index.html ru/providers/microvm/index.html \
                   assets/stylesheets/extra.css assets/images/under-construction.svg \
                   search/search_index.json; do
            test -s "site/$f"
          done
          grep -q 'ws-hero' site/index.html
          grep -q 'class="mermaid"' site/index.html
          grep -q 'extra.css' site/ru/index.html
          grep -q 'hreflang="ru"' site/index.html
          grep -q 'lang="ru"' site/ru/index.html
          grep -q 'ws-steps' site/get-started/index.html
          grep -q 'tabbed-set' site/ru/get-started/index.html
          grep -q 'md-status--soon' site/providers/index.html
          grep -q 'under-construction.svg' site/ru/providers/microvm/index.html
          grep -q 'class="mermaid"' site/concepts/architecture/index.html
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
